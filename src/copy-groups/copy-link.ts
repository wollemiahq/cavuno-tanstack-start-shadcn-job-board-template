import { m } from '../paraglide/messages';

export function copyLinkCopy() {
  return {
    ariaLabel: m.copyLink_ariaLabel(),
    copiedLabel: m.copyLink_copiedLabel(),
    copyLinkLabel: m.copyLink_copyLinkLabel(),
  };
}
