/**
 * robots.txt / ads.txt / indexnow-key.txt response builders plus the Google
 * site-verification meta descriptor — re-exported from the SDK so this
 * starter cannot drift from hosted / Worker bytes.
 */
export {
  adsTxtResponse,
  googleSiteVerificationMeta,
  indexNowResponse,
  robotsResponse,
} from '@cavuno/board/seo-files';
