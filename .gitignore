node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.dev.vars.example
!.env.example

# Paraglide compiler output — generated from messages/ (which come from uiCopy)
src/paraglide

# Agent scratch worktrees — never part of the template
.claude/worktrees/
